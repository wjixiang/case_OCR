import caseProcess from '../src/caseProcess';

const testCaseData = [
    "test"
]

describe.skip("process primary ocr text to complete digital case",()=>{
    const processer = new caseProcess(testCaseData)

    test("set up AIclient",()=>{
        
    })
})

